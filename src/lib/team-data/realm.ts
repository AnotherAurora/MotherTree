/** Stable id of the chaos realm row (`realm` table). Prefer this over `name`. */
export const CHAOS_REALM_ID = 1;

/** Stable id of the aequor realm row. */
export const AEQUOR_REALM_ID = 4;

/** Stable id of the benthos aequor realm row (`replace` → aequor). */
export const BENTHOS_AEQUOR_REALM_ID = 5;
